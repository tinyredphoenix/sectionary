rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /laws/{law} {
      allow read: if true;
      allow write: if false; // Only allow writes via backend/admin SDKs for now
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}